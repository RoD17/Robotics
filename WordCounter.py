def count(sequence, item):
cont=0
for i in sequence:
    if item==i:
        cont+=1
return cont

def interate(string):
	if(string != null)
		word = string.split(maxsplit=1)
		count count(string, word)
		return print("'" + word + "': " + count ", ")

string = raw_input("Enter a string:")

interate(%s)
